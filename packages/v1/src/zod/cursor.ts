// whereFrom // 定位下一页起始点
// 单列排序时:
// 获取游标之后的行的条件表达式。对于多列 ORDER BY，用 OR + AND 组合成复合条件。示例在对 col1 ASC, col2 DESC 且游标值为 [v1, v2] 时生成：
// (col1 > v1) OR (col1 = v1 AND col2 < v2)
// 这样能正确处理多列排序的边界。

// whereCurrent
// 处理重复值
// 如果有很多行的 createdAt 完全一样，游标就需要一种机制来精准定位到你刚才离开的那个位置。这部分通常用于同步逻辑（Sync Layers），确保数据不重不漏
// 定位到等于当前游标值的行（仅用第一排序列）。用于处理边界上的重复值。对普通值就是 eq(col1, v1)，对 Date 等连续类型则用 AND(gte(...), lt(...)) 范围匹配防止精度问题
