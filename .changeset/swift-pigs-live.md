---
'agnostic-query': minor
---

Ship compiled dist exports for subpath entrypoints, add a build step to prepublish, and align db0 query types so `all()` supports both async and sync result providers.
