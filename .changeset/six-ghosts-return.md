---
"agnostic-query": patch
---

fix: replace non-portable `./*`/`./*/*` exports with `"./"` directory pattern for broader compatibility
chore: remove unused `@tanstack/query-db-collection` devDependency
